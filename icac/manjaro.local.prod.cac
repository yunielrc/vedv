#!/usr/bin/env bash
set -eu

# Install prod dependencies
sudo pacman -Syu --noconfirm --needed yay

yay -Syu --noconfirm --needed \
  virtualbox \
  hadolint-bin \
  gnu-netcat \
  python-pip \
  libxml2 # /usr/bin/xmllint is owned by libxml2

sudo -H pip install --break-system-packages dockerfile-parse
sudo -H pip install --break-system-packages petname
